Ext.define('Eway.view.version.VersionInstallInfo', {
	extend : 'Ext.Panel',
	alias : 'widget.versionInstallInfo',
	
	title : '版本安装信息统计图',

	initComponent : function() {
		Ext.apply(this, {
			
		});
		this.callParent(arguments);
	}
});