Ext.define('Ext.ux.button.SortButton',{

	extend : 'Ext.button.Button',
	alias : 'widget.sortButton',
	
	/*config : {
		direction : 'ASC',
		dataIndex : undefined
	},
	
	constructor : function(config){
//		this.addEvents('changeDirection');
//		this.initConfig(config);
//		this.callParent(arguments);
	},
	handler : function(){
		this.toggleDirection();
	},
	
	updateDirection : function(direction){
		this.setIconCls('direction-'+direction.toLowerCase());
		this.fireEvent('changeDirection',direction,this);
	},
	toggleDirection : function(){
		this.setDirection(Ext.String.toggle(this.direction,"ASC","DESC"));
	}*/
	
	
});